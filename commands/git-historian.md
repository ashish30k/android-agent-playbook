---
description: Reconstruct why weird code is shaped that way before "fixing" it
argument-hint: [file or code location]
---
The code at $ARGUMENTS looks wrong or weird. Before proposing a fix, act as a git historian: use git log -p and blame to reconstruct why it's shaped this way — the bug it fixed, the constraint that existed. Then tell me whether that constraint still holds. Chesterton's fence, automated.
